{
	"extends": "../../../tsconfig.base.json",
	// TypeScript 7 no longer pulls in every @types package it finds under
	// node_modules, so @types/node is named explicitly here -- the dev server
	// under dev-server/ is the part of this package that needs it.
	"compilerOptions": {
		"types": ["node"]
	}
}
