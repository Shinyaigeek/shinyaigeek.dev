/**
 * The experiments the labs site publishes, in the order the top page lists
 * them. Each one also needs its pages registered in build.ts -- they have no
 * shared shape to generate from, since the point of an experiment is that it
 * does something the others do not.
 */
export const PROJECTS = ["prerender2"];
